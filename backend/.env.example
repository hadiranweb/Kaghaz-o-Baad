# Liara / runtime
NODE_ENV=production
HOST=0.0.0.0
PORT=8080

# Required core secrets
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
DATABASE_SSL=false
AUTH_JWT_SECRET=replace-with-a-random-secret-at-least-32-characters
CORS_ORIGIN=https://<frontend-app-domain>

# AI usage gateway
AI_PROVIDER=openai
AI_API_KEY=replace-with-ai-provider-key
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-5-mini
AI_TIMEOUT_MS=30000
AI_CACHE_ENABLED=true
AI_TITLE_CACHE_TTL_SECONDS=86400
AI_TITLE_PROMPT_VERSION=title-v1

# Rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_IP_PER_MINUTE=120
RATE_LIMIT_USER_PER_MINUTE=60
RATE_LIMIT_AI_PER_MINUTE=10

# Billing / ZarinPal
ZARINPAL_MERCHANT_ID=replace-with-zarinpal-merchant-uuid
ZARINPAL_SANDBOX=true
PAYMENT_CALLBACK_BASE_URL=https://<backend-app-domain>
PAYMENT_PROVIDER_TIMEOUT_MS=30000

# LiveKit: required for live sessions
LIVEKIT_URL=https://<livekit-host>
LIVEKIT_API_KEY=replace-with-livekit-api-key
LIVEKIT_API_SECRET=replace-with-livekit-api-secret

# Liara Object Storage (S3-compatible)
S3_ENDPOINT=https://<object-storage-endpoint>
S3_REGION=us-east-1
S3_BUCKET=kaghazbaad-media-staging
S3_ACCESS_KEY_ID=replace-with-storage-access-key
S3_SECRET_ACCESS_KEY=replace-with-storage-secret-key

# SMS.ir Verify / OTP (server-side only)
SMSIR_API_KEY=replace-with-smsir-api-key
SMSIR_TEMPLATE_ID=replace-with-smsir-template-id
SMSIR_CODE_PARAMETER=CODE
SMSIR_TIMEOUT_MS=10000
OTP_TTL_SECONDS=120
OTP_MAX_ATTEMPTS=5

# Email verification / Liara Mail SMTP (server-side only)
# Liara Mail SMTP host and implicit TLS port, per official documentation.
EMAIL_PROVIDER=smtp
EMAIL_FROM=verified@kaghazobaad.ir
SMTP_HOST=smtp.c1.liara.email
SMTP_PORT=465
SMTP_USER=replace-with-liara-smtp-username
SMTP_PASSWORD=replace-with-liara-smtp-password
# Leave empty when EMAIL_PROVIDER=smtp.
RESEND_API_KEY=
EMAIL_VERIFICATION_TTL_SECONDS=1800

# PaaS AI/automation adapters (optional until the independent apps are provisioned)
N8N_BASE_URL=https://n8n.<your-domain>
N8N_EVENT_WEBHOOK_URL=https://n8n.<your-domain>/webhook/kaghazbaad-events
N8N_WEBHOOK_SECRET=replace-with-a-random-secret-at-least-32-characters
OPENCLAW_BASE_URL=https://agent.<your-domain>
OPENCLAW_GATEWAY_TOKEN=replace-with-a-random-secret-at-least-32-characters
OPENWEBUI_BASE_URL=https://ai.<your-domain>

# Optional job tuning
CACHE_CLEANUP_BATCH_SIZE=100
SUBSCRIPTION_GRACE_DAYS=3
SUBSCRIPTION_JOB_BATCH_SIZE=100
